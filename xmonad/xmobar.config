Config {
    font = "xft:Envy Code R:pixelsize=14",
    bgColor = "#000000",
    fgColor = "#DDDDDD",
    commands = [
        Run Date "%H:%M" "date" 600,
        Run StdinReader
    ],
    template = "%StdinReader% }{ <fc=#6294CF>%date%</fc>"
}
